---
const { frontmatter } = Astro.props;
import '../styles/globals.css';
---

<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{frontmatter.title}</title>
    <meta name="description" content={frontmatter.description || "Brayan's curated list of projects and portfolio website. Crafted with love and passion."} />
    <meta name="keywords" content="web development, front-end, full-stack, design, portfolio, blog, UI, UX" />
    <meta name="author" content="Brayan Mejia Cuenca" />
    <meta property="og:title" content={frontmatter.title} />
    <meta property="og:description" content="Brayan's personal blog and portfolio website. I write about web development, design, and other topics." />
    <meta property="og:url" content={frontmatter.image} />
    <meta property="og:image" content="/mejiabrayan.webp" />
  </head>

  <body class="bg-[#08090B] text-accents-7">
    <div class="max-w-3xl mx-auto px-4 sm:px-6 lg:px-8 py-12">
      <main>
        <article class="prose prose-invert prose-lg max-w-none sm:prose-xl lg:prose-xl">
          <header class="mb-8">
            <img
              class="w-full h-64 object-cover rounded-xl mb-6"
              src={frontmatter.cover}
              alt={frontmatter.title}
              draggable="false"
              loading="lazy"
            />
            <h1 class="text-3xl sm:text-4xl lg:text-5xl font-bold text-white mb-4">{frontmatter.title}</h1>
            {frontmatter.link && (
              <a
                href={frontmatter.link}
                target="_blank"
                rel="noopener noreferrer"
                class="text-primary-accent hover:underline"
              >
                Visit Project
              </a>
            )}
          </header>

          <div class="text-accents-7 prose-headings:text-white prose-a:text-primary-accent prose-strong:text-accents-5 prose-code:text-accents-6 prose-pre:bg-accents-9 prose-blockquote:border-l-primary-accent prose-blockquote:text-accents-6">
            <slot />
          </div>
        </article>
      </main>
    </div>
  </body>
</html>